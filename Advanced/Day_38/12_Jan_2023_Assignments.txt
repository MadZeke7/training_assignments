1. What is useRef()? Why do we use it?
A) useRef() is a React hook to store variables that will be saved across renders and wont trigger re-renders. It is used for storing variables such as "id" or a variable that doesn't need to be displayed on the screen.